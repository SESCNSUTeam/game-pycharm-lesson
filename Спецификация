Протокол отправки:

    отправка от клиента:

        {id : [[events], flag]}

            id - id клиета отправителя
            [events] = [[key_down], [key_up], [mouse], [cast]]
                -//-



            flag - флаг сессии:
                0 - первое подключение (первый пакет данных после поделючения)
                1 - нормальная сессия (обычный пакет данных)
                2 - отключение (последний пакет, после обработки которого сервер должен отправить даные кленту об отключении)

    отправка от сервера:

        {id : [data]}

            id - id клиета получателя

                [data] = [{obj}, **[special]] - информация об изменениях в игре

                    [obj] - список объектом с ключом равным id объекта

                        каждый объект описывается так:

                        {obj_id : int(), act_id: int(), pos : (x, y, rot), class_id : int(), **animation_id : int()}

                            obj_id - id обЪекта

                            act_id - id действия с объектом
                                0 - оздание объекта
                                1 - удаления объекта
                                2 - перемещение объекта
                                3 - создание объекта с привязкой к нему камеры

                            pos - позиция объекта
                                x - позиция по x
                                y - позиция по y
                                rot - поворот вокруг центра против часовой стрелки в градусах

                            class_id - id класса (нужно для загрузки необходимой картинки)


                    **[special] - массив данных об изменях в игре не связаных с объектами


* - необязательное для любого пакета
** - пока не реализовывать

