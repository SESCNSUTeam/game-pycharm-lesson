Client:
	Client(хост, порт(можно один и тот же для всех), имя клиента)
	Client.start():
		Запуск клиента
	Client.get_input(): 
		Возвращает словарь вида {имя клиента: []},
		где в списке хранится все, что клиент успел получить с момента предыдущего вызова
	Client.add_output_data(list):
		Принмает на вход список всего, что нужно отправить

Server:
	Server(хост, порт)
	Srever.start():
		Запуск сервера
	Server.get_input():
		При каждом вызве возвращает словарь вида {1: [], 2 [], 3: [] ...}
		1, 2, 3 ... номера клиентов
		В списках хранится все, что клиент успел получить с момента предыдущего вызова
	Server.add_output_data(list):
		Принимает на вход список всего, что нужно отправить